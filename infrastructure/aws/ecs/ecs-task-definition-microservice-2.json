{
  "family": "microservice-2",
  "containerDefinitions": [
    {
      "name": "microservice-2-container",
      "image": "${ECR_URL_MICROSERVICE_2}:latest",
      "memory": 512,
      "cpu": 256,
      "essential": true,
      "portMappings": [
        {
          "containerPort": 5001,
          "hostPort": 5001,
          "protocol": "tcp"
        }
      ],
      "environment": [
        {
          "name": "QUEUE_URL",
          "value": "${SQS_QUEUE_URL}"
        },
        {
          "name": "BUCKET_NAME",
          "value": "${S3_BUCKET_NAME}"
        },
        {
          "name": "PULL_INTERVAL",
          "value": "5"
        },
        {
          "name": "AWS_REGION",
          "value": "us-east-2"
        }
      ]
    }
  ],
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "${ECS_TASK_EXECUTION_ROLE_ARN}",
  "taskRoleArn": "${ECS_TASK_ROLE_ARN}"
}
